# Agent Definition

## 共通規則
- このプロジェクトの共通規則は `../AGENTS/AGENTS.md`。作業前に読む。
- `../AGENTS` がない端末では `https://github.com/goenc/ANDROID_AGENTS.git` を同階層の `AGENTS` へ clone する。既存の場合は変更前に GitHub と同期する。
- このリポジトリも変更前に pull し、変更後に日本語 commit、push、remote SHA 照合まで行う。詳細は共通規則に従う。
