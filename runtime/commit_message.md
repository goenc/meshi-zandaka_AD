グラフ枠を横並び向けに縮小

理由:
カード1枚内で日別棒を横並び表示する構成に合わせ、グラフ枠の縦方向の占有を減らすため

変更点:
- 日別棒の高さを160dpから96dpへ縮小
- グラフカードの上下余白と内部間隔を縮小
- 日別棒内の縦方向の間隔を縮小

確認内容:
- .\gradlew.bat testDebugUnitTest 成功
- .\gradlew.bat assembleDebug 成功
- git diff --check 成功
- 実機へadb install -rで更新
- 凡例6項目が同じ行に並ぶことを確認
- グラフ枠と下段サマリーが画面内に収まることを確認
